[workspace]
resolver = "2"
members = [
    "pgr-core",
    "pgr-input",
    "pgr-search",
    "pgr-display",
    "pgr-keys",
    "pgr-cli",
]


[profile.release]
lto = true
strip = true
panic = "abort"
codegen-units = 1
opt-level = "s"
